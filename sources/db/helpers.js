var Sequelize = require( __willikins_core_modules + '/sequelize' );

export var fn = Sequelize.fn;
export var literal = Sequelize.literal;

export var and = Sequelize.and;
export var or = Sequelize.or;
