#!/usr/bin/env bash

UNIX_SCRIPT=$(tr '\\' '/' <<< "$0")
UNIX_PATH=$(cd "$(dirname "$UNIX_SCRIPT")" ; pwd -P)

NPM_MODULES=$(npm root)
NODE_PATH="${NPM_MODULES};:${NPM_MODULES}:;${NODE_PATH}"

export NODE_PATH

node "${UNIX_PATH}"/../sources/_setup "$@"
