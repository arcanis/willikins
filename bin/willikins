#!/usr/bin/env bash

NPM_MODULES=$(npm root)
NODE_PATH="${NPM_MODULES};:${NPM_MODULES}:;${NODE_PATH}"

export NODE_PATH

SCRIPT_PATH=$(dirname "$(readlink -f "$0")")
node "${SCRIPT_PATH}"/../sources/_setup "$@"
